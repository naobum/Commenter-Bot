{
  "PublicBaseUrl": "https://your.domain",
  "ConnectionStrings": {
    "sqlite": "Data Source=/data/memory.db;Cache=Shared;Mode=ReadWriteCreate"
  },
  "Bot": {
    "TelegramToken": "${TELEGRAM_BOT_TOKEN}",
    "WebhookSecretPathSegment": "${WEBHOOK_SECRET}",
    "AllowedChatIdsCsv": "${ALLOWED_CHAT_ID_CSV}",
    "LlmBaseUrl": "https://api.openai.com",
    "LlmApiKey": "${OPENAI_API_KEY}",
    "LlmModel": "openai/gpt-5",
    "MaxContextMessages": 20,
    "ReplyProbability": 0.3
  }
}